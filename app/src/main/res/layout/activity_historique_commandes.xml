<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF"
    tools:context=".HistoriqueCommandesActivity">

    <!-- BARRE ROUGE EN HAUT -->
    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbarHistorique"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="#D32F2F"
        android:elevation="4dp"
        app:title="Historique des commandes"
        app:titleTextColor="#FFFFFF"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- MESSAGE SI AUCUNE COMMANDE -->
    <LinearLayout
        android:id="@+id/layoutAucuneCommande"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ImageView
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:src="@android:drawable/ic_menu_recent_history"
            android:alpha="0.3"
            app:tint="#D32F2F" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Aucune commande pour le moment"
            android:textSize="18sp"
            android:textColor="#999999"
            android:layout_marginTop="24dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Vos commandes apparaîtront ici"
            android:textSize="14sp"
            android:textColor="#CCCCCC"
            android:layout_marginTop="8dp" />
    </LinearLayout>

    <!-- LISTE DES COMMANDES -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewHistorique"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:paddingTop="8dp"
        android:paddingBottom="8dp"
        android:clipToPadding="false"
        app:layout_constraintTop_toBottomOf="@id/toolbarHistorique"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:listitem="@layout/item_commande_historique" />

</androidx.constraintlayout.widget.ConstraintLayout>